package UI.Buttons;

import javax.swing.*;
import java.awt.*;

public class AddButton extends JButton {
    public AddButton() {
        super("Add");
        setBackground(Color.GREEN);
        setBorder(new RoundedBorder(20));
    }
}
