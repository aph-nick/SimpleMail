package UI.Buttons;

import javax.swing.*;
import java.awt.*;

public class CancelButton extends JButton {
    public CancelButton() {
        super("Cancel");
        setBackground(Color.white);
        setBorder(new RoundedBorder(20));
    }
}
